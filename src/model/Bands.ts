export type Bands = {
    id: string;
    name: string;
    music_genre: string;
    responsible: string;
}

export type BandDTO = {
    name: string;
    music_genre: string;
    responsible: string;
}